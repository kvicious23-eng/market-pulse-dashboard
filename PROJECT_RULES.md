# Market Pulse 운영·계산 기준

이 문서는 Market Pulse의 단일 기준 문서(Single Source of Truth)다. 새 작업은 이 문서를 먼저 확인하고, 문서와 코드·화면·수집 결과가 충돌하면 임의로 해석하지 말고 충돌 사실과 영향을 먼저 보고한다. 요구사항이 바뀌면 코드와 함께 이 문서도 갱신한다.

Git 커밋 이력은 언제 무엇을 변경했는지 보존하고, 이 문서는 그 변경들을 반영한 현재 유효 규칙만 유지한다. 단순 가격 데이터 갱신은 문서에 작업일지처럼 누적하지 않지만, 계산·수집·표시·운영 방식이 바뀌면 반드시 이 문서도 함께 갱신한다.

사용자가 매번 별도로 요청하지 않아도, 수정과 검증이 완료되면 작업자는 완료 보고 전에 `PROJECT_RULES.md` 반영 필요 여부를 스스로 판단하고 갱신한다. 기준·동작·완료 조건에 영향을 주는 변경은 문서 반영까지 끝나야 완료된 것으로 본다.

## 1. 현재 기준선

- 공개 저장소: `kvicious23-eng/market-pulse-dashboard`
- Windows 설치 경로: `C:\MarketPulse`
- Chrome 확장프로그램 코드 버전: `1.9.7` (저가 상품 허용 및 저가 상품의 관련 없는 고액 취소선 배제). 운영 PC에서 코드를 받은 뒤 확장프로그램을 새로고침해야 다음 수집에 적용된다.
- 대시보드 경로: Lenovo `/brand/lenovo/`, Acer `/brand/acer/`; 신규 브랜드 Godox `/brand/godox/`는 첫 정상 수집·업로드 뒤 자동 생성된다.
- 모든 브랜드의 정식 경로와 데이터는 `/brand/{slug}/index.html`, `/brand/{slug}/market-data.js` 구조만 사용한다. `/`는 `/brand/lenovo/`, `/acer/`는 `/brand/acer/`로 즉시 이동하며 별도 화면·데이터·수집 경로를 갖지 않는다.
- 현재 활성 상품: Lenovo 3개, Acer 10개, Godox C100(Camera, SRP 42,000원) 1개로 총 14개. 2026-09-24 오전 1.9.6 수집 JSON에서 14개 완료를 확인했지만 업로드는 신규 브랜드 메타데이터 속성 오류로 중단되어 대시보드 반영은 아직 검증되지 않았다.
- 마지막 전체 성공 검증: 2026-09-23 확장프로그램 1.9.3 수동 수집, Lenovo 3개·Acer 10개 등 전체 13개 상품을 누락·중복·상세구분 미확인·가격 계산 불일치 없이 수집·배포 성공
- 마지막 주문서 할인 성공 검증: 2026-09-23 확장프로그램 1.9.3으로 구매 가능 11개 상품의 쿠폰 3종 수집 성공, 품절 2개 상품 분리 성공, SFG16-I71-75Y2 카드할인 16,790원·최종가 1,662,210원으로 재계산 일치
- 최신 실제 회차 검증: 2026-09-23 확장프로그램 1.9.4의 14:00 회차 결과가 14:00:17~14:15:50 KST에 13개를 수집했다. 원래 구매 가능 11개 정상, Acer 1개 품절, SFG16-I71-75Y2 1개는 와우회원 총 추가 혜택에 카드 할인이 포함된 탓에 보류로 분류됐다. 보관된 **같은 회차의 JSON**을 1.9.5 계산 규칙으로 교차 검증하여 해당 1개를 정정했다(별도의 재수집은 아님). 정정 후 14시 회차는 구매 가능 12개 정상·품절 1개다. 14:30 예약 업로드는 PowerShell의 Git 출력 오류 처리 때문에 실패했고, 코드를 수정한 뒤 PC에서 직접 재실행했다. **수정 후 예약 작업의 무인 업로드 성공 및 1.9.5의 실제 주문서 재수집은 아직 검증되지 않았다.**
- 가격은 수집 시점의 쿠팡 화면 및 팝업 근거만 사용한다. 과거 값이나 추정값으로 현재 값을 만들어내지 않는다.

버전과 상품 수는 기준선 기록이며, 이후 정상 변경 시 실제 설정값과 함께 이 문서를 갱신한다.

## 2. 가격 용어와 계산식

| 항목 | 정의 및 계산 |
|---|---|
| SRP | 관리화면에서 사용자가 직접 입력하는 기준가격. 수집기가 생성하거나 판매가로 대체하지 않는다. |
| 표시가 | 신뢰 가능한 취소선 가격. 취소선이 없으면 상품 가격 영역에서 가장 상단에 표시된 가격. 내부 계산의 기준가격과 같은 값이다. |
| 매칭차액 | `표시가 - SRP`. SRP 또는 표시가가 없으면 `미확인`이며, 표시가가 SRP보다 낮으면 음수 차액으로 표시한다. |
| 상품페이지 할인가 | 상품 상세페이지에서 표시가보다 할인되어 보이는 가격. 구매 버튼이 없거나 비활성인 품절 상품의 일반쿠폰 계산에만 사용한다. |
| 카드할인 전 가격 | `표시가 - 일반 쿠폰할인 - 와우 전용 즉시할인 - 와우 전용 쿠폰할인`. |
| 쿠폰할인 총금액 | `표시가 - 카드할인 전 가격`. 유효한 두 값이 있고 표시가가 카드할인 전 가격보다 크거나 같을 때 계산한다. |
| 일반 쿠폰할인 | 구매 가능 상품은 주문/결제 화면의 `쿠폰할인` 금액이다. 구매 버튼이 없거나 비활성인 품절 상품만 `표시가 - 상품페이지 할인가`로 계산한다. |
| 와우 전용 즉시할인 | 구매 가능한 상품의 주문/결제 화면에서 `와우전용 즉시할인` 금액을 수집한다. |
| 와우 전용 쿠폰할인 | 구매 가능한 상품의 주문/결제 화면에서 `와우전용 쿠폰할인` 금액을 수집한다. |
| 카드할인 | 적용 카드 중 가장 큰 유효 할인액. `min(카드할인 전 가격 × 할인율, 최대 할인한도)`. 최대 한도 표기가 없으면 할인율 계산액을 사용한다. 원 단위 미만은 버린다. |
| 최종 실구매가 | `카드할인 전 가격 - 카드할인`. 카드 혜택 없음이면 카드할인 0원이다. |

표시가와 SRP의 대소관계와 무관하게 이후 계산은 중단하지 않는다. 예를 들어 SRP가 4,099,000원이고 표시가가 3,102,430원이면 매칭차액은 `-996,570원`이며, 쿠폰과 카드할인은 표시가 3,102,430원을 시작점으로 계속 계산한다.

SRP가 비어 있으면 판매가를 대신 표시하지 않고 반드시 `SRP 미입력`으로 표시한다.

## 3. 쿠팡 가격 수집 원칙

1. 등록 상품의 정확한 `productId`·`itemId`·`vendorItemId`가 모두 선택된 상태에서만 값을 인정한다. 세 값 중 하나라도 URL과 다르면 가격·카드·바로구매 결과를 사용하지 않는다.
2. 표시가는 신뢰 가능한 취소선 가격을 우선한다.
3. 취소선 가격이 없으면 상품 가격 영역에서 화면상 가장 상단의 가격을 표시가로 사용하고 `priceBasisType`을 최상단 가격으로 기록한다.
4. 쿠팡의 추천상품, 다른 옵션, 함께 구매, 적립금, 캐시, 보험·보증연장 금액은 가격 후보에서 제외한다.
5. JSON-LD, 화면 텍스트, 선택된 옵션 및 URL의 `itemId`가 서로 충돌하면 임의 선택하지 않고 미수집 또는 미확인으로 처리하며 근거를 남긴다.
6. 수집 실패를 가격 변동으로 판정하지 않는다.

관리 SRP가 250,000원 미만인 상품은 상품페이지·다나와 판매가와 가져오기 단계에서 10,000~7,000,000원의 가격 후보를 허용한다. 그 외 상품은 종전처럼 250,000~7,000,000원만 허용한다. 이 하한은 화면에 표시된 정확한 상품 식별자·가격 근거 및 주문서·카드 검증을 대신하지 않는다.

저가 상품에서 취소선 후보가 SRP와 선택된 상품페이지 가격 양쪽의 3배를 모두 초과하면 해당 후보를 표시가로 사용하지 않는다. 이전 버전 JSON을 가져올 때도 같은 상한을 적용한다. JSON-LD의 활성 가격과 화면 가격이 일치하고, 주문서 할인 3종이 모두 확인된 0원인 경우에만 그 가격을 최상단 가격으로 다시 판정한다. 근거가 모자라면 현재 최종가를 검증 완료로 표시하지 않는다. 2026-09-24 Godox C100의 41,160원 활성 가격에 붙은 1,249,570원 취소선은 이 규칙에 따라 제외한다.

## 4. 카드할인 수집과 상태

쿠팡 상품 가격 영역에 `카드 즉시할인` 안내문이 없으면 카드 혜택이 종료된 것으로 판단한다. 안내문이 있으면 안내문 오른쪽의 해당 정보 아이콘만 클릭하고 팝업 상세정보를 확인한다.

정보 아이콘은 안내문 실제 글자 좌표와 화면 기하를 기준으로 식별한다. 무관한 가격·안심케어·적립 정보 아이콘은 클릭하지 않는다. 클릭 후 일반 DOM뿐 아니라 Accessibility Tree와 DOM Snapshot을 함께 확인하고, 팝업의 할인율·최대한도·적용 카드사를 교차 검증한다.

| 내부 상태 | 화면 의미 | 계산 처리 |
|---|---|---|
| `none` | 혜택 없음 | 카드할인 0원, 최종 실구매가는 카드할인 전 가격과 동일 |
| `partial` / `unverified` | 안내문은 있으나 상세정보 미수집 또는 근거 충돌 | 카드할인과 최종 실구매가를 임의 계산하지 않고 `미확인` |
| `captured` | 상세정보 수집 완료 | 검증된 할인율·한도·카드사로 계산 |

- 카드사가 하나라도 적용되면 카드할인을 적용한다.
- 카드사별 할인율·최대한도가 다르면 실제 할인액이 가장 큰 유효 조건을 사용한다.
- 최대한도 표기가 없다는 사실이 확인되면 할인율만으로 계산한다.
- 안내문에 보이는 `최대 N%`만으로는 `captured`로 판정하지 않는다. 팝업 상세 근거를 확보해야 한다.
- 카드 혜택은 수시로 생성·종료되므로 수집 시각과 근거를 함께 보존한다.
- 공개 데이터의 카드 근거 문구에는 할인율·카드사 요약만 보존한다. 추적 URL, 이미지 URL, CSS, 쿠팡캐시·쿠페이 홍보 문구는 제거한다.

## 5. 구매 가능 여부에 따른 쿠폰 3종 수집

- 카드할인은 상품페이지의 카드 혜택 팝업에서만 수집하며 주문서 할인과 섞지 않는다.
- `바로구매` 버튼이 없거나 비활성화되어 있으면 상품을 `품절`로 기록한다. 이때 일반쿠폰만 상품페이지의 `표시가 - 상품페이지 할인가`로 계산하고, 와우 즉시·와우 쿠폰과 현재 최종 실구매가는 만들지 않는다.
- `바로구매`가 활성화된 모든 상품은 상품페이지의 쿠폰 추정값을 현재 일반쿠폰으로 사용하지 않고 주문/결제 페이지에 반드시 진입한다.
- 주문서 진입은 정확한 `itemId`, 수량 1개, 안심케어 미선택을 확인한 별도 탭에서 `바로구매`를 한 번만 클릭한다.
- 주문/결제 페이지는 제목과 `결제하기` 버튼으로 확인한다. 확인 이후에는 읽기만 하며 `결제하기`, 옵션, 배송지, 결제수단을 클릭하거나 변경하지 않는다.
- 주문서의 `쿠폰할인`은 대시보드의 `일반 쿠폰`, `와우전용 즉시할인`은 `와우 즉시`, `와우전용 쿠폰할인`은 `와우 쿠폰`으로 각각 저장한다.
- 주문서의 일반쿠폰은 `쿠폰할인`, `쿠폰할인 변경`, `일반 쿠폰할인`, `상품 쿠폰할인` 문구를 같은 항목으로 인식하되 `와우 전용 쿠폰할인` 및 `와우전용 쿠폰할인`과 섞지 않는다. 와우 즉시·와우 쿠폰은 `와우 전용`과 `와우전용` 공백 변형을 모두 인식한다.
- 주문서에 항상 표시되는 단독 `쿠폰할인` 섹션 제목은 일반쿠폰 적용 근거가 아니다. 같은 행에 금액이 있거나 `변경` 제어가 있는 `쿠폰할인` 행, 또는 `일반 쿠폰할인`·`상품 쿠폰할인` 행만 일반쿠폰 존재로 판정한다. 섹션 제목만 있고 실제 행이 없으면 일반쿠폰은 `0원`이다.
- 주문/결제 페이지 진입이 확인된 뒤 할인 영역을 최대 4회 판독한다. 세 항목 중 끝까지 화면에 없는 항목은 각각 `0원`으로 확정하며, 세 항목이 모두 없어도 모두 `0원`으로 기록한다.
- 주문서에 할인 라벨이 존재하지만 해당 금액을 판독하지 못하면 그 항목을 `0원`으로 바꾸지 않고 주문서 전체 할인을 `unverified`로 처리한다. 여러 할인 행이 하나의 부모 영역에 묶여도 각 라벨 다음 금액을 다음 할인 라벨 전까지만 판독한다.
- 주문서 할인 금액은 같은 할인 행에서 라벨 뒤 금액을 우선하고, 쿠팡이 금액을 먼저 렌더링하거나 라벨·금액을 형제 요소로 분리한 경우에는 같은 행의 라벨 앞 금액도 판독한다. 다른 할인 라벨의 경계를 넘어선 금액은 사용하지 않는다.
- 할인 3종 중 일부만 판독하지 못한 경우에도 정상 판독된 항목과 화면에 존재하지 않아 0원으로 확정된 항목은 진단값으로 보존한다. 다만 전체 상태는 `unverified`이며 최종 실구매가와 가격 비교에는 사용하지 않는다.
- 주문서 판독 실패 시 실패한 필드명, 항목별 판독 상태 및 개인정보를 제거한 할인 근거 문구를 수집 JSON과 브랜드 데이터에 보존한다.
- 주문서 진입, 로딩 또는 주문/결제 페이지 확인에 실패한 경우에는 세 항목을 0원이나 상품페이지 추정값으로 바꾸지 않고 `missing` 또는 `unverified`로 처리한다.
- 주문서의 `와우회원 총 추가 혜택`과 `와우 전용 카드 즉시할인`은 쿠폰 3종이나 최종가 계산·검증에 사용하지 않는다. 총 추가 혜택은 카드 할인을 포함할 수 있으므로 쿠폰 합계와 불일치해도 수집 보류 사유가 아니다. 카드 할인은 오직 상품페이지 ⓘ 팝업에서 수집·계산·검증하며, 주문서 카드 할인 금액을 쿠폰에 섞거나 최종가에서 다시 빼지 않는다. 카드 행의 금액을 인접 쿠폰 행의 금액으로 오인하지 않도록 라벨 경계만 구분한다.
- 구매 가능 상품의 최종 층별 검증식은 `쿠폰할인 총금액 = 주문서 일반쿠폰 + 와우 즉시 + 와우 쿠폰`, `카드할인 전 가격 = 표시가 - 쿠폰할인 총금액`이다. 품절 상품의 일반쿠폰만 `표시가 - 상품페이지 할인가`다.
- 주문서 실패와 상품 품절은 서로 다른 상태로 보존한다.
- 품절 상품의 와우 즉시·와우 쿠폰은 `미수집`이나 `0원`이 아니라 `품절로 미적용`으로 표시한다.
- 주소·연락처·결제수단·카드번호·쿠팡캐시 등 개인정보와 결제정보는 수집하지 않는다. 진단 근거에는 할인 라벨과 금액 문구만 제한적으로 기록한다.
- 진단 탭은 비활성 백그라운드 탭으로 열고 완료 또는 실패 후 닫는다. 실제 주문서 호스트 `https://checkout.coupang.com/*`에는 읽기 접근만 사용한다.

## 6. 대시보드 및 PRICE EVIDENCE

`내 쿠팡상품` 표와 PRICE EVIDENCE, Excel 내보내기는 동일한 계산 결과를 사용해야 한다. 다음 항목을 빠짐없이 표시한다.

- SRP
- 표시가
- 표시가 종류: 취소선 또는 최상단
- 매칭차액
- 일반 쿠폰할인
- 와우 전용 즉시할인
- 와우 전용 쿠폰할인
- 쿠폰할인 총금액
- 카드할인 전 가격
- 카드할인 상태
- 카드할인 금액
- 적용 카드사
- 할인율
- 최대 할인한도 또는 `한도 표기 없음`
- 최종 실구매가
- 수집·확인 시각과 근거 출처

카드할인 상태가 `partial` 또는 `unverified`이면 0원으로 오인시키지 않고 `상세정보 미수집` 또는 `미확인`으로 보여준다. `none`만 0원이다.

- 내 상품과 경쟁 상품의 현재 `finalPrice`는 `alertEligible: true`가 명시되고, 내 상품은 카드 상태가 `none` 또는 `captured`이며 당일 정상 수집으로 확인된 경우에만 표시·비교한다. 값이 없거나 `false`인 경우뿐 아니라 필드 자체가 없는 과거 데이터도 현재가로 인정하지 않는다.
- 정상 수집 결과를 가져오면 `alertEligible: true`, 실패하여 이전 검증값을 보존하면 `alertEligible: false`로 기록한다. `false`인 과거값은 보존하되 현재 실구매가·가격 비교·변동 알림에 사용하지 않는다.
- `lastVerifiedFinalPrice`와 `lastVerifiedPriceCheckedAt`는 마지막 정상 수집값을 별도로 보존한다. 중간 실패·부분 수집·품절 뒤 다시 정상 수집되더라도 직전 정상값과 가격 변동을 비교한다.
- 현재 수집이 검증 보류라면 보존된 과거 최종가로 현재 쿠폰 합계나 카드할인 전 가격을 역산해 보여주지 않는다. 현재 할인 상세가 확인되지 않았으면 `미확인`으로 표시한다.
- 경쟁사 가격은 내 상품과 같은 수집일이고 `alertEligible: true`인 값만 현재 비교·알림에 사용한다. 경쟁사 수집 실패 시 과거값은 보존하되 `alertEligible: false`로 전환한다. GitHub 수동 경쟁가 조사는 Chrome이 검증한 내 상품의 상태·가격·`alertEligible`을 변경하지 않는다.
- 다나와 등 외부 출처의 조건부 카드가, 과거 참고가, `conditionalBest` 및 `conditionalLabel`은 현재 쿠팡 실구매가 옆에 표시하지 않는다.
- 카드사명·할인율·한도·카드 적용가는 정확한 쿠팡 Item ID의 현재 카드 팝업에서 `captured`로 검증된 경우에만 현재 혜택으로 표시한다.

Excel 내보내기 형식은 `.xlsx`이며, 화면과 같은 항목 및 값을 사용한다. 활성 상품 수와 `3개`, `10개` 같은 분모는 고정 문자열이 아니라 실제 활성 상품 수에서 계산한다.

- `MTM OVERVIEW`는 경쟁사 비교를 반복하지 않는다. 각 MTM 행에서 상태, SRP·표시가·매칭차액, 쿠폰할인 3종과 합계, 카드할인 전 가격·상태·금액·조건·카드사, 최종 실구매가와 확인 시각을 클릭 없이 모두 표시한다.
- 현재 상품의 상태가 `현재가 직접 확인`이고 비교 가능한 현재가(`alertEligible: true`)인 경우에만 녹색 태그를 사용한다. `품절`, 주문서 미확인·부분 수집, 보존된 과거가 등 나머지 상태는 `MTM OVERVIEW`와 `SELLER COMPARISON` 모두 품절과 같은 빨간색 태그로 표시한다. 경쟁 상품도 현재가를 검증할 수 없으면 빨간색 태그로 표시한다.
- `MTM OVERVIEW`의 최종 실구매가는 직전 정상 수집값과 비교한다. 현재가가 낮아지면 행을 옅은 빨강, 높아지면 옅은 노랑으로 표시하고 변동액을 함께 표시한다. 동일하거나 비교 가능한 직전 정상값이 없으면 기존 녹색·중립 표시를 유지한다. 수집 실패로 보존된 과거값은 변동 기준으로 사용하지 않는다.
- `SELLER COMPARISON`는 가격 기준과 할인 상세를 각각 묶어 데스크톱 한 화면에서 PRICE EVIDENCE 버튼까지 볼 수 있게 한다. 개별 할인 항목은 묶음 안에서 모두 유지한다.

수동·자동 수집 결과는 수집 PC의 `C:\MarketPulse\reports\my-coupang-price-history.csv`에 수집 날짜·시각, 브랜드, MTM을 키로 누적한다. 최초 기록은 2026-09-23 09:20:52 KST의 13개 상품이며 이전 기록은 제거한다. 같은 수집 시각의 같은 MTM은 중복 저장하지 않는다. 원장에는 내 쿠팡상품의 가격·할인·상태만 포함하며 경쟁사 가격은 포함하지 않는다. 이 CSV는 Excel에서 직접 열 수 있다. 파일이 아직 없다면 `CREATE_PRICE_HISTORY.cmd`가 현재 배포 스냅샷으로 최초 원장을 만들고, 이후 수집부터 같은 파일에 자동 누적한다. 가져오기 때 기준 시각 이전 행을 제거하고 공개 대시보드용 `dist/price-history.js`를 생성한다. Lenovo와 Acer 대시보드의 상단 다운로드 버튼은 히스토리 스크립트가 로드되어 행이 있을 때 활성화되며, 두 브랜드의 히스토리 전체와 상품 URL을 `.xlsx`로 제공한다. 이 데이터는 공개 대시보드 방문자도 내려받을 수 있다.

2026-09-23 14:15:50 KST 회차의 SFG16-I71-75Y2는 기존 1.9.4 수집기가 와우회원 총 추가 혜택 166,790원에 카드 할인 16,790원이 들어간 것을 쿠폰 합계 불일치로 오판했다. 원본 JSON의 주문서 와우 쿠폰 150,000원, 상품페이지 표시가 1,829,000원·할인가 1,679,000원, 상품페이지 ⓘ 카드 할인 16,790원을 교차 검증하여 **동일 수집 시각의 실패 행을 정정**했다. `scripts/history-corrections.json`에 원본 JSON의 SHA-256과 정정값을 남긴다. 다음 정상 업로드 시 PC에만 남은 실패 CSV 행도 같은 키와 원래 필드가 정확히 일치할 때만 정정하여, 대시보드 다운로드가 다시 실패값으로 덮이지 않게 한다. 이 정정은 새로운 수집 결과나 버전 1.9.5의 실측 성공으로 간주하지 않는다.

`LATEST MARKET SNAPSHOT`의 주제는 내 쿠팡상품의 직전 정상 수집 대비 최종 실구매가 하락·상승과 현재 품절 개수다. 비교할 직전 정상값이 없거나 현재 수집이 실패한 상품은 변동 없음으로 세지 않고 비교 불가로 표시한다. 경쟁 판매처 가격 역전은 이 제목의 근거로 사용하지 않는다.

## 7. 상품·브랜드 관리

- 관리화면에서 기존 상품 수정과 신규 상품 저장을 지원한다.
- 상품 기본 필드는 브랜드, 모델명(MTM), 쿠팡 URL, `productId`, `itemId`, `vendorItemId`, SRP, 활성 여부이며 필요 시 다나와 URL을 포함한다.
- 신규 브랜드는 저장 직후 빈 대시보드를 억지로 만들지 않는다. 다음 정상 수집·업로드에서 공통 템플릿으로 `/brand/{slug}/` 대시보드를 자동 생성한다. 브랜드 제목의 품목 분류는 관리 카탈로그의 `category`를 사용하며, 여러 분류가 섞인 브랜드는 `Products`로 표시한다.
- 브랜드 슬러그는 소문자로 만들고 공백·특수문자를 `-`로 정규화한다. Lenovo·Acer·향후 브랜드 모두 같은 폴더 구조와 생성 로직을 사용한다.
- Lenovo와 Acer도 예외 페이지가 아니라 같은 공통 구조를 사용한다.
- 브랜드별 화면, PRICE EVIDENCE, Excel 규칙은 모두 동일하다.
- 상품 카탈로그의 활성 상태가 대시보드 모델 수와 수집 대상을 결정한다.

## 8. 실패·보존·업로드 원칙

- 현재 수집이 실패하면 이전에 검증된 가격을 삭제하거나 새 값으로 덮어쓰지 않는다.
- 이전 검증값을 보존하더라도 현재 수집 실패 상태와 시각을 명확히 표시하고, 그 값을 새 가격 변동 알림의 근거로 사용하지 않는다.
- 사용자가 수집 중 Chrome 창이나 마우스를 조작해 발생한 실패는 가격 오류와 구분한다.
- 업로드 전에 상품 수, `itemId`, 가격 관계, 카드 상태, 계산식 및 결과를 검증한다.
- 당일 생성되고 24시간을 넘지 않은 정상 결과만 자동 업로드 대상으로 삼고 오래된 JSON을 재업로드하지 않는다.
- 스캔 파일은 확장 버전, 시작·완료 시각, 대상 수, 결과 수, 완료 여부를 기록한다. 대상 수·결과 수·완료 표시가 없거나, 대상 수와 결과 수가 다르거나, Item ID가 중복되거나, 완료 표시가 거짓이면 가져오기를 중단한다.
- 가져오기 시 `productId`·`itemId`·`vendorItemId`를 함께 대조한다. 등록 상품과 식별자가 맞지 않거나 활성 상품 결과가 누락되면 기존 값을 보존하고 현재 비교·알림 대상에서 제외한다.
- 주문서 세부 할인이 `captured`가 아니거나 카드 상태가 `captured`/`none`이 아니면 최종 실구매가를 현재 비교값으로 공개하지 않는다. 품절 상품도 최종 실구매가와 변동 계산에서 제외한다.
- 코드 변경·배포와 가격 데이터 업로드는 구분한다. 검증되지 않은 값은 공개 대시보드에 배포하지 않는다.
- 수집 결과의 `checkoutDiscountReason`이 `buy-now-button-not-found` 또는 `buy-now-button-sold-out`이면 해당 MTM을 `품절`로 표시하고 `alertEligible: false`로 처리한다. `checkout-page-not-confirmed` 등 주문서 기술 실패는 품절로 간주하지 않는다.

## 9. Windows 자동 실행

- 설치 위치: `C:\MarketPulse`
- Windows 예약 작업 `Market Pulse Chrome Start`: PC 현지시각 매일 08:00과 14:00. 한국 운영 PC의 표준 시간대는 `(UTC+09:00) 서울`로 유지한다.
- 확장프로그램 자동 수집: 매일 08:00과 14:00 KST. Chrome이 닫혀 있으면 위 예약 작업이 Chrome을 시작하고, Chrome이 실행 중이면 확장프로그램 알람이 해당 회차 수집을 시작한다.
- 각 자동 수집에는 상품페이지 표시가·카드할인, 구매 가능 여부 판정, 구매 가능 상품의 주문서 쿠폰 3종, 품절 상품의 상품페이지 일반쿠폰 수집이 포함된다.
- 오전·오후 회차는 날짜와 회차 시각을 합친 실행 슬롯으로 구분한다. 같은 회차의 중복 실행은 막고, 오전 수집 완료 여부가 오후 수집을 막지 않는다.
- Windows 예약 작업 `Market Pulse Result Upload`: PC 현지시각 매일 08:30과 14:30에 각 회차의 최신 정상 결과를 가져와 업로드한다.
- 예약 업로더는 현재 회차의 08:00 또는 14:00 KST 이후에 시작한 수집만 받는다. 오후 수집이 실패하거나 늦으면 오전 파일로 오후 회차를 대체하지 않고 기다린 뒤 실패를 기록한다.
- 예약 업로드가 실패하면 15분 간격으로 최대 3회 다시 실행한다. 이전 실행에서 로컬 커밋까지 성공하고 푸시만 실패했어도 다음 실행은 원격 푸시를 다시 시도한다.
- 이미 가져온 같은 수집 파일의 푸시를 재시도할 때는 가격을 다시 계산하지 않아 직전 정상 수집 대비 변동을 보존한다.
- 예약 업로드 시작·완료·실패 로그는 `C:\MarketPulse\reports\scheduled-upload.log`에 기록한다.
- Windows PowerShell에서 Git의 정상적인 표준 오류 출력(`From ...` 등)을 업로드 실패로 취급하지 않는다. 실제 Git 종료 코드로 성공 여부를 판정하고, 전체 실행 기록을 예약 업로드 로그에 남긴다.
- GitHub Actions의 08:00 서버 조사는 실측 PC 수집과 중복되므로 사용하지 않는다. 정기 원천은 Windows PC 확장프로그램의 08:00·14:00 수집과 08:30·14:30 업로드다.
- 두 예약 작업은 `WakeToRun`과 `StartWhenAvailable`을 사용한다. 절전 상태에서는 깨우기 타이머로 실행하고, 완전히 종료되어 있었다면 다음 부팅·로그인 뒤 누락 작업을 실행한다.
- 정상 운영 확장은 `C:\MarketPulse\chrome-extension` 하나만 사용한다. 과거 `%LOCALAPPDATA%\MarketPulseDashboard` 확장은 중복 실행 방지를 위해 비활성화하거나 제거한다.
- 자동 수집 중 사용자가 Chrome 창을 조작하면 탭 선택이나 팝업 수집이 방해될 수 있으므로 완료까지 조작하지 않는다.

## 10. 재고 기능 제외 원칙

- Market Pulse는 가격과 할인만 수집·표시·처리한다. 재고, SKU ID, Supplier Hub 연동은 운영 범위에 포함하지 않는다.
- 관리화면, 대시보드, 수집 결과, 업로더, Excel, 설치 및 예약 작업 어디에도 재고 필드나 재고 수집 절차를 추가하지 않는다.
- 별도 재고 수량은 수집하지 않는다. 다만 등록 상품 화면의 `바로구매` 버튼이 없거나 비활성화된 직접 확인 결과는 구매 가능 상태 판정으로 사용하며, 대시보드에 연한 빨강 배경·빨강 글자의 `품절` 상태로 표시한다. 다른 가격·주문서 수집 실패는 품절로 추정하지 않는다.

## 11. 카드 계산 회귀검증 사례

2026-09-16에 전체 성공한 수집 결과를 카드 로직 회귀검증 기준으로 사용한다.

| MTM | 카드할인 전 가격 | 할인율 | 최대 한도 | 카드할인 | 최종 실구매가 |
|---|---:|---:|---:|---:|---:|
| ANV16-I31-514Z | 1,330,190원 | 8% | 109,060원 | 106,415원 | 1,223,775원 |
| PHN16S-71-949J | 3,032,430원 | 1% | 31,020원 | 30,324원 | 3,002,106원 |
| SFG16-I71-75Y2 | 1,679,000원 | 1% | 16,790원 | 16,790원 | 1,662,210원 |

추가 가격 회귀검증 사례:

- AG14-I71M-972S: SRP 1,429,000원, 표시가 1,413,470원, 매칭차액 -15,530원, 카드할인 전 가격 1,259,000원, 쿠폰할인 총금액 154,470원.
- PHN16S-71-949J: SRP 4,099,000원, 표시가 3,102,430원, 매칭차액 -996,570원.
- SF16-71T-7475: 취소선 가격이 없을 때 최상단 가격 2,369,000원을 표시가로 사용한다. `바로구매`가 없거나 비활성인 품절 상태에서 상품페이지 할인가가 2,159,000원이면 일반쿠폰은 210,000원이며, 현재 최종 실구매가는 표시하지 않는다. 구매 가능 상태라면 일반쿠폰은 상품페이지 차액이 아니라 주문서 `쿠폰할인`을 사용한다.

회귀검증 사례는 계산 코드의 정확성 확인용이다. 이후 현재 가격으로 재사용하지 않는다.

## 12. 변경 완료 조건

변경은 다음을 모두 만족해야 완료로 본다.

1. 관련 코드와 이 문서가 같은 규칙을 말한다.
2. 구문 검사와 가능한 자동 검증을 통과한다.
3. 가격 로직 변경은 위 회귀검증 사례로 계산 결과를 확인한다.
4. 브랜드 공통 구조와 Lenovo/Acer 양쪽에 동일하게 적용된다.
5. 공개 `main` 반영 여부와 커밋을 확인한다.
6. 실제 쿠팡 화면 의존 변경은 다음 정상 수집 결과로 최종 확인하며, 확인 전에는 이론상 완료와 실측 완료를 구분해 보고한다.
7. 수정·검증 결과가 계산·수집·표시·운영 기준 또는 현재 기준선을 바꾸면, 사용자 요청을 기다리지 않고 `PROJECT_RULES.md`를 함께 갱신한다.


## 13. 수집 무결성 및 배포 검증

- 가져오기 파일은 payload version 5와 Chrome 확장프로그램 1.9.3 이상만 허용한다.
- 각 결과의 Product ID, Item ID, VendorItem ID 세 값이 모두 존재하고 관리 카탈로그와 정확히 일치해야 한다.
- 스캔 시작·완료·대표 시각과 각 상품·주문서 확인 시각은 같은 수집 구간에 있어야 한다. 역전된 시각, 3시간을 초과한 수집, 서로 다른 수집 파일이 섞인 결과는 가져오지 않는다.
- 구매 가능 상품은 `표시가 - 일반쿠폰 - 와우 즉시 - 와우 쿠폰`으로 재계산한 카드할인 전 가격이 상품페이지에서 직접 확인한 가격과 정확히 같아야 한다. 다르면 `unverified`로 처리하고 현재 최종가를 공개하지 않는다.
- 카드 상태가 `captured`이면 수집 JSON의 카드할인 금액과 업로더가 할인율·한도로 다시 계산한 금액이 같아야 한다. 카드 혜택 없음은 카드할인 0원과 모순되지 않아야 한다.
- 신규 브랜드명은 정규화된 URL 슬러그가 서로 겹치면 가져오기를 중단하고, 같은 브랜드·MTM 또는 VendorItem ID 중복도 허용하지 않는다.
- 신규 브랜드에는 기존 브랜드와 같은 경쟁 조사 시각 메타데이터를 생성한다. 메타데이터가 비어 있거나 이전 형식이어도 속성 갱신 오류로 전체 업로드가 중단되지 않게 한다.
- 주문/결제 페이지가 확인된 뒤 최대 4회 판독해도 화면에 없는 일반쿠폰·와우 즉시·와우 쿠폰 항목만 각각 0원으로 기록한다. 주문서 확인 실패는 0원으로 바꾸지 않는다.
- 주문서의 와우회원 추가 혜택 합계와 카드 할인 행은 계산·교차 검증에서 제외한다. 주문서 일반쿠폰·와우 즉시·와우 쿠폰의 각 행과 상품페이지 카드 팝업만 각각의 계산 근거로 삼는다.
- `바로구매` 버튼이 없거나 비활성화된 상품은 품절로 기록한다.
- 기술적 수집 실패 행은 히스토리에 실패 사실만 남기고 과거 가격을 새 수집 시각의 가격처럼 복제하지 않는다.
- 배포 워크플로는 JavaScript·PowerShell 구문과 시장 데이터 구조·가격 계산 관계를 검증한 뒤에만 GitHub Pages를 배포한다.
- 브랜드별 데이터 파일은 `/brand/{slug}/market-data.js`만 정본으로 사용한다. 기존 `dist/market-data.js`와 `acer/market-data.js`는 생성·동기화·검증·배포하지 않는다.
